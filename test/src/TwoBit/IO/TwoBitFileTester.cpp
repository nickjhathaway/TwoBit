#include <catch.hpp>
#include "../src/TwoBit/IO/TwoBitFile.hpp"

/*
TEST_CASE("Basic tests for TwoBitFile", "[void TwoBitFile (TwoBit::TwoBitFile& other)]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/

/*
TEST_CASE("Basic tests for TwoBitFile", "[void TwoBitFile ()]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/

/*
TEST_CASE("Basic tests for operator=", "[TwoBitFile & operator= (TwoBit::TwoBitFile& other)]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/

/*
TEST_CASE("Basic tests for TwoBitFile", "[void TwoBitFile (std::string& filename)]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/

/*
TEST_CASE("Basic tests for operator[]", "[TwoBitSequence operator[] (std::string& s)]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/

/*
TEST_CASE("Basic tests for sequenceNames", "[const std::vector<std::string> & sequenceNames ()]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/

/*
TEST_CASE("Basic tests for size", "[const uint32_t size ()]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/
