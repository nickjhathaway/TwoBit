#include <catch.hpp>
#include "../src/TwoBit/IO/fasta.hpp"

/*
TEST_CASE("Basic tests for readNextFasta", "[bool readNextFasta (std::istream& in,std::unique_ptr<FastaRecord>& seq)]" ){
  SECTION("GIVE SECTION NAME"){
      YOUR CODE GOES HERE
        NORMALLY END WITH A REQUIRE STATEMENT e.g.
        REQUIRE(TESTVAL1 == YOURVAL);
  }
}
*/
